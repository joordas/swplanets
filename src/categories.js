// this document declares what will be queried to the API and rendered to the categories picker (first item of each array, string) and which details are going to be extracted from the request (second item, array of strings);

const categories = [
  ["planets", ["climate", "terrain", "population", "films"]],
  ["people", ["eye_color", "height", "films"]]
];

export default categories;
